// UserProfile — public profile showing uploaded books, reading history, bio (Phase 2)

interface UserProfileProps {
  userId: string;
}

export default function UserProfile({ userId }: UserProfileProps) {
  // TODO: Implement user profile with reading history, followers/following, and genres
  return <div>{/* User profile for {userId} coming soon */}</div>;
}
